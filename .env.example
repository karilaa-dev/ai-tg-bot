# Required: Telegram BotFather token.
BOT_TOKEN=123456:replace-me

# Required: E2B API key. The bot uses one persistent custom E2B Base sandbox per Telegram thread.
E2B_API_KEY=replace-me

# Required: OpenRouter fallback/image/embedding key and Tavily search key.
OPENROUTER_API_KEY=replace-me
TAVILY_API_KEY=replace-me

# Optional Camofox server. When configured, interactive browser tools, direct Telegram
# screenshot/file delivery, and model-only OfficeCLI previews are enabled.
# web_search always remains on Tavily.
# WEB_EXTRACT_PROVIDER=tavily
# CAMOFOX_URL=http://camofox.example:9377
# CAMOFOX_ACCESS_KEY=replace-me
# CAMOFOX_TIMEOUT_MS=30000
# CAMOFOX_DEPLOYMENT_ID=ai-tg-bot

# E2B defaults. Change DEPLOYMENT_ID when two bot deployments share one E2B account.
# Build this reusable template once with `npm run e2b:template:build`.
# E2B_TEMPLATE=ai-tg-bot-tools:production
# E2B_DEPLOYMENT_ID=ai-tg-bot
# E2B_REQUEST_TIMEOUT_MS=30000
# TELEGRAM_FILE_RESTORE_TIMEOUT_MS=300000
# TELEGRAM_FILE_RESTORE_CONCURRENCY=4

# Optional runtime config.
# DB_URL=sqlite:./data/bot.db
# PI_CODING_AGENT_DIR=./data/pi
# MODEL_CONTEXT_TOKENS=128000
# PI_THINKING_LEVEL=medium
# PI_TURN_TIMEOUT_MS=900000
# THREAD_TITLE_TIMEOUT_MS=30000

# Optional: Codex OAuth-backed Pi models.
# CODEX_MODEL=gpt-5.6-sol
# CODEX_HELPER_MODEL=gpt-5.6-luna

# Optional: OpenRouter models.
# OPENROUTER_MAIN_MODEL=openai/gpt-5.6-sol
# OPENROUTER_HELPER_MODEL=openai/gpt-5.6-luna
# OPENROUTER_IMAGE_MODEL=openai/gpt-5.4-image-2
# IMAGE_TIMEOUT_MS=300000
# OPENROUTER_EMBEDDING_MODEL=perplexity/pplx-embed-v1-0.6b

# Optional external Docling server for DOCX, scanned PDFs, and image-only PDFs.
# DOCLING_URL=https://docling.example
# DOCLING_TIMEOUT_MS=300000

# Large-file retrieval.
# FILE_INLINE_TOKENS=6000

# Sandbox command limits.
# BASH_TIMEOUT_MS=30000
# BASH_MAX_OUTPUT_CHARS=12000

# Telegram and logging behavior.
# DRAFT_UPDATE_MS=0
# ONBOARDING_TIMEZONE_DELAY_MS=2000
# LOG_LEVEL=info

# Container identity and Compose PostgreSQL.
# APP_UID=1000
# APP_GID=1000
POSTGRES_PASSWORD=replace-with-a-long-random-password
