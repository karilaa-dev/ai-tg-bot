# AI Telegram Bot

AI-powered Telegram bot with streaming responses, tool support, and conversation persistence.

## Setup

1. Copy `.env.example` to `.env` and fill in API keys
2. Install dependencies: `uv sync`
3. Run: `python main.py`
